
LINK_DIRECTORIES(${pde_bd_BINARY_DIR}/lib)

include_directories(${Boost_INCLUDE_DIRS} ${PYTHON_INCLUDE_DIRS} ${PDE_BD_INCLUDE_DIRECTORIES})
PYTHON_ADD_MODULE(pypdb python.cpp)

TARGET_LINK_LIBRARIES(pypdb pdb ${Boost_LIBRARIES})
